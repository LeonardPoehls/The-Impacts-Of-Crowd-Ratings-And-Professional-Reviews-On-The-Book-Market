ps.pkg.info = function() {
  # Name of this package
  package = "RTutorImpactsOnBookMarket"
  
  # Name of problem sets in the package
  ps = c("The Impacts of reviews and Ratings on Book Market")
  
  list(package=package, ps = ps)
}
